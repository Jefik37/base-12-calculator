# base 12 converter
Simple base 12 calculator, written in python. There might be some bugs, but most of it should be working properly.

It might not work on Mac and Linux because of the Clipboard button. If it doesn't work, changing the copy() function around line 215 should fix it.
